{% extends 'base.html.twig' %}

{% block title %}User List{% endblock %}

{% block stylesheets %}
    <style>
        body, html {
            height: 100%;
            margin: 0;
        }

        body {
            background: linear-gradient(to bottom, black, grey);
            color: white;
            display: flex;
            flex-direction: column;
            min-height: 100%;
        }

        .container {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }


    </style>
{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4 text-center">User List</h1>
        <table class="table table-striped table-bordered table-dark table-hover">
            <thead>
            <tr>
                <th>ID</th>
                <th>Nume</th>
                <th>Email</th>
                <th>Data Nașterii</th>
                <th>Sex</th>
            </tr>
            </thead>
            <tbody>
            {% for user in users %}
                <tr>
                    <td>{{ user.id }}</td>
                    <td>{{ user.nume }}</td>
                    <td>{{ user.mail }}</td>
                    <td>{{ user.birthDay ? user.birthDay|date('d-m-Y') : 'N/A' }}</td>
                    <td>{{ user.gender == 0 ? 'Masculin' : user.gender == 1 ? 'Feminin' : user.gender == 2 ? 'Other' : 'Prefer not to say' }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
